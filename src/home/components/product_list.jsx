function ProductList({title, product_list}) {
    return (
        <section>
            <h3>{title}</h3>
            <div>
                products
            </div>
        </section>
    )
}

export default ProductList